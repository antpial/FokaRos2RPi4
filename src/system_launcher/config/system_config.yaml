sensor_ph:
  ros__parameters:
    ########################### BE CAREFULL ##########################
    publish_frequency: 2.0 # Must be NOT faster than the aggregator!!!
    ##################################################################

sensor_thermometer:
  ros__parameters:
    ########################### BE CAREFULL ##########################
    publish_frequency: 2.0 # Must be NOT faster than the aggregator!!!
    ##################################################################  

aggregator:
  ros__parameters:
    ############################### BE CAREFULL ##############################
    publish_frequency: 2.0 # Must be at least as fast as the fastest sensor!!!
    ##########################################################################
